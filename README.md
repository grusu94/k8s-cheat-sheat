# k8s-cheat-sheat

I. Install kubectl and minikube
```bash
winget install -e --id Kubernetes.kubectl
winget install -e --id Kubernetes.minikube
```
II. Start minikube
```bash
minikube start --driver=hyperv
```
